# file
  : 파일 종류 조회
# touch 
  : 시간 속성 변경, 존재하지 않는 파일일 시 empty 파일 생성
# rm(remove)
  : 파일 삭제
    -i : 삭제하시겠습니까 질문
    -r : 하위 디렉토리까지 전부 삭제
    -f : 디렉토리 내 파일 존재 여부 상관없이 삭제
# cp(copy)
  : 파일 복사(옵션은 rm과 동일)
# mv(move)
  : 파일 이동(이름 바꾸기 가능)
# rename
  : 파일 이름 변경(주로 mv 사용)
